stages:
 - build
build_hello:
 stage: build
 image: gcc:latest   # Use official GCC compiler image
 script:
   - gcc hello.c -o hello
   - echo "Build completed successfully"
 artifacts:
   paths:
     - hello
